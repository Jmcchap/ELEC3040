int cycle = 0;
int button = 0;

//actLEDs ()

/*KeypadInterrupt ()
{
  //set button value (0 through 9)
  actLEDs(button, led1, led2, led3, led4);
  cycle = button;}
*/

/*TimeBasedInterrupt ()
{
  if (cycle == 0)
  {
    //set output to low (0)
    cycle = button;
  }
  else
  {
    //set output to high (1)
    cycle--;
  }
}
*/

/*main ()
{
  //infinite loop
}
*/
