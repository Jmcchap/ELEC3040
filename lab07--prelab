int cycle = 0;
int button = 0;

//actLEDs ()

/*KeypadInterrupt ()
{
  //set button value (0 through 9)
  actLEDs(button, led1, led2, led3, led4);
  }
*/

/*TimeBasedInterrupt ()
{
  if (cycle >= button)
  {
    //set output to low (0)
    cycle++;
  }
  else if (cycle == 9)
  {
    //set output to low (0)
    cycle = 0;
  }
}
*/

/*main ()
{
  //infinite loop
}
*/
